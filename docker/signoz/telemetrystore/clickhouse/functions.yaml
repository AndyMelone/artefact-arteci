functions:
  - name: histogramQuantile
    type: executable
    command: ./histogramQuantile
    format: CSV
    return_type: Float64
    argument:
      - name: buckets
        type: Array(Float64)
      - name: counts
        type: Array(Float64)
      - name: quantile
        type: Array(Float64)
